import React from "react";
import dummy from "../../db/data.json";

const Clubcard = () => {
  console.log(dummy.data);
  return <>카드</>; /*dummy.dat.map((data) => console.log(data));*/
};

export default Clubcard;
